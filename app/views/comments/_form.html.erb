<%= form_with(model: [@post, comment]) do |form| %>
  <% if comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comment.errors.count, "error") %> no permitió que se guardara el comentario:</h2>

      <ul>
        <% comment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, "Comentario:", style: "display: block" %>
    <%= form.text_area :content, rows: 5 %>
  </div>

  <div>
    <%= form.submit "Publicar comentario", class: "btn btn-primary" %>
  </div>
<% end %>

  </div>
<% end %>
